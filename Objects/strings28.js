/* str.substr(start, [, length]), возвращает часть
строки от start длины length */

let str = "stringify"; 
// ring, получаем 4 символа, начиная с позиции 2
console.log(str.substr(4,4));

console.log(str.substr(-4,2));
// gi, получаем 2 символа, начиная с позиции 4 
// с конца строки