/* Вызов в контексте массива */

let arr = [ "a", "b" ];

arr.push(function() { console.log(this) });

arr[2](); // [ 'a', 'b', [Function (anonymous)] ]


/* Вызов arr[2]() синтаксически - старый добрый
obj[method](), в роли obj - arr, а в роли method - 2,
Итак, у нас есть вызов функции arr[2] как метода объекта.
Соответственно, он получает в качестве this объект
arr и выводит массив: */

