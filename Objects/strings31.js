/* Сделаем строку, содержащую символы с кодами от 65
до 220 - это латиница и ещё некоторые распространённые
символы: */

let str = '';

for (let i = 65; i < 220; i++) {
    str += String.fromCodePoint(i);
};

console.log(str);