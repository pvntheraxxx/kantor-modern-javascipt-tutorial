/* Очевидным решением было бы проверить значение с помощью
if или условного оператора ?, прежде чем обращаться к 
его свойству, вот так: */

let user = {};
console.log(user.address ? user.address.street : undefined);

// Это работает, тут нет ошибки
// Но это довольно неэлегантно