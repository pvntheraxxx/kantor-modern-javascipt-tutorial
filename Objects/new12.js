/* Две функции - один объект */

let obj = {};

function A() { return obj; };
function B() { return obj; };

console.log(new A() == new B()); // true

// Результаты конструкторов A() и B() равны
// потому что они возвращают один и тот же объект
// они возвращают один и тот же объект из памяти, obj
