/* Незаметно подменяем переменную на нечто похожее, например: */

function ninjaFunction(elem) {
    // 20 строк кода, работающего с elems

    elem = clone(elem);
    // ещё 20 строк кода, работающего с elem!
}