/* Уровней вложенности не должно быть много. Например,
в цикле бывает полезно использовать директиву continue, 
чтобы избежать лишней вложенности. Например, вместо 
добавления вложенного условия if, как здесь: */

const a = 1
cond = a > 0
for (let i = 0; i < 10; i++) {
    if (cond) {
        // <- ещё один уровень вложенности
        console.log(a);
    }
}