/* Максимальную длину строки согласовывают в команде.
Обычно это 80 или 120 символов */

const id = 123;
const moonPhase = 'Waning Gibbous';
const zodiacSign = 'Libra';

if (
    id === 123 &&
    moonPhase === 'Waning Gibbous' &&
    zodiacSign === 'Libra'
) {
    console.log("Yes");
} else {
    console.log("No");
}