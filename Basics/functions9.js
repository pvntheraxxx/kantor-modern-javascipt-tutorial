/* В качестве параметров по умолчанию можно указать
в том числе и функцию и метод */

function showMessage(from,text = anotherFunction()) {
    // anotherFunction() выполнится только если не передан text
    // результатом будет значение
}

/* Вывод: в приведённом выше примере, функция anotherFunction()
не будет вызвана вообще, если указан параметр text. В качестве
передаваемых аргументов могут быть другие функции, которые 
возвращают некоторый результат. */