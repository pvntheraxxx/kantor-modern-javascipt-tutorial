/* Цикл 'for...in' используется для перебора всех 
перечисляемых свойств объекта. Это как если бы мы 
прошлись по каждой карточке в коробке с карточками. */

for (let key in user) {
    // действия с каждым свойством объекта
};