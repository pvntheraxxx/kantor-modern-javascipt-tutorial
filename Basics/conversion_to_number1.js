/* Необходимость преобразовывать строки в числа 
возникает очень часто. Например, обычно значения
полей HTML-формы - это строки. А что, если нужно,
к примеру, сложит? */
let apples = "2";
let oranges = "3";
console.log(apples + oranges); // "23", т.к. бинарный плюс объединяет строки
