/* Как и в случае с "ИЛИ", любое значение допускается 
в качестве операнда "И" */

if (1 && 0) { // вычисляется как true && false
    console.log("не сработает, так как результат ложный");
} else {
    console.log("будет выполнено это, так как условие - false");
}