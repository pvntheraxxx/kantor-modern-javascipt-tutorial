/* Проверка значения вне диапазона. Напишите условие if для проверки, 
что значение переменной age НЕ находится в диапазоне 14 и
90 включительно. "Включительно" означает, что значение переменной age
может быть равно 14 или 90. */
age = 11;
if (!(age >= 14 && age <= 90)) {
    console.log("Right");
} else {
    console.log("Wrong");
}