/* Как и оператор ИЛИ ||, И && иногда может заменять if */
let x = 1;

(x > 0) && console.log("Greater than zero!");
/* Действия в правой части && выполнится только в том
случае, если до него дойдут вычисления. То есть
console.log() сработает, если в левой части (x > 0) 
будет true. */