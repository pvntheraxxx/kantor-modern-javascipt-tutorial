/* Метка имеет вид идентификатора с двоеточием перед циклом. Вызов break 
<labelName> в цикле ниже ищет ближайший внешний цикл с такой меткой и переходит
в его конец */

outer: for (let i = 0; i < 3; i++) {
    for (let j = 0; j < 3; j++) {
        console.log(`Значение на координатах (${i},${j})`);
        // если i равно 2, то выйти из обоих циклов
        if(i == 2) break outer; // (*)
        // сделать что-нибудь со значениями...ы
    }
}
console.log("Готово!");