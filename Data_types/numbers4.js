/* Метод toFixed(n) округляет число до n знаков после
запятой и возвращает строковое представление результата. */

const num = 12.35;
console.log(num.toFixed(1)); // "12.3"
console.log(typeof num.toFixed(1)); // strings
