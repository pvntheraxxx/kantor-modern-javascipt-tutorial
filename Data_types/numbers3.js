/* Чтобы округлить число до второго знака после запятой,
можно умножить число на 100, вызвать функцию округления
и разделить обратно. */

const num = 1.23456; 
console.log(Math.floor(num * 100)/100);
// 1.23456 -> 123.456 -> 123 -> 1.23